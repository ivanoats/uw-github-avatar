/** @jsx React.DOM */
// does not work now that app is jsx?
// var app = require('../../app/js/app');

describe('Home Page', function() {

  it('getGithubData was called', function() {
    expect(false).toBeFalsy();
  });
});
